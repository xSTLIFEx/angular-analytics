import { GoogleAnalyticsEventDirective } from './google-analytics-event.directive';

describe('GoogleAnalyticsEventDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleAnalyticsEventDirective();
    expect(directive).toBeTruthy();
  });
});
